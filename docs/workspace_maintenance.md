# 工作区维护规则

## 维护主口径

- 后续新增规则，优先改仓库文档和脚本，不要只留在聊天里
- 发布、回滚、GitHub 备份、跨工作区协作，统一以仓库内脚本和配置为准
- 当前工作区如果依赖其他工作区，只能连接对方正式入口
- 涉及千帆后台的流程，默认必须保持只读、低频、最小操作；如果要查订单信息，优先按单店分批执行，并保持不规则停顿，不允许固定节奏高频查询
- 涉及千帆后台的默认风控口径，以 `config/xhs_qianfan_guardrails.json` 和 `docs/xhs_qianfan_safety.md` 为准；后续所有千帆需求默认沿用这套极度保守规则
- “好评表缺失 SKU 补齐”这条链路固定统一走订单查询页 `https://ark.xiaohongshu.com/app-order/order/query`，不同店铺只切 Chrome profile，不再按店铺拆不同页面
- 这条 SKU 链路的店铺 profile 正式映射固定看 `config/xhs_order_query_profiles.json`；后续新增店铺优先补配置，不要改主流程
- 这条 SKU 链路的规格标准化映射也固定收敛到 `config/xhs_order_query_profiles.json`；后续新增商品规格优先补配置，不要把映射散落到不同脚本
- 这条 SKU 链路的正式交互主备固定为 `AX -> browser_js -> mouse`：`AX` 是长期主方案，`browser_js` 只是增强模式，鼠标只作最后兜底
- 这条 SKU 链路的正式窗口绑定固定按“店铺 profile + 统一订单查询页 URL”重绑目标窗口，不依赖用户当前 tab
- “好评表已上评同步”这条链路固定走评价管理页按日期导出，不走逐单订单搜索；默认 `14:00` 主跑，只有主跑失败时 `14:20` 才补跑，正式安装入口固定是 `bash scripts/install_review_status_launchagent.sh`
- 这条链路的正式交互主方案固定为：复用现有 Chrome 店铺资料打开评价页，再用 `AX` 无鼠标控件操作完成日期填写、搜索和“全部导出”；正常路径不抢鼠标
- `browser_js` 仍保留在脚本里作为显式验证模式，但不再作为这条正式定时链路的默认主方案
- 点完“全部导出”后，先给 `60` 秒确认下载是否真的开始；如果首轮明显没开始，会先关掉本轮页面，重新打开干净页面后再按 `AX` 重跑一轮
- 只有“首轮 AX 没开始导出 + 重开页面后再跑仍失败”时，才允许降级到现有鼠标流兜底；鼠标流不是长期主方案，只是最后保底
- 这条定时安装脚本必须同时写入 `PATH` 和 `REVIEW_STATUS_PYTHON_BIN`，固定使用带 `pyautogui` 的 Python 3.11，避免 `launchd` 落回系统 Python
- 这条正式任务不要求页面预先打开；页面被关掉时，默认先复用对应店铺的 Chrome 资料重开评价管理页
- 这条正式任务不再假设“旧评价页窗口一定还在”。维护时默认按“窗口可能被用户手动关掉、Chrome 可能有多个主进程”处理：每轮都要重新扫描可见窗口，按目标 URL 和店铺文本重绑正确窗口，必要时自己重开页面
- 如果本机关闭了 Chrome 的“允许 Apple 事件中的 JavaScript”，正式链路也必须能继续工作；页内 JS 只能算增强能力，不能再被当成唯一依赖
- 活动蒙层、公告弹窗这类阻断层，只允许点“关闭 / 取消 / 稍后 / 跳过”这类安全控件，严禁点“去参与 / 去开通 / 去设置 / 确认提交”这类正向按钮
- 这条正式任务结束后要自动删除本轮评价导出临时文件，不在桌面和运行目录留残留
- 这条正式任务结束后只允许关闭“本轮自己新开的评价页窗口”：以任务开始前后窗口集合差异为准，逐个按窗口 ID 关闭；如果任务前窗口快照缺失，必须放弃自动关窗，避免误关用户原窗口。关窗失败要明确记录残留窗口，不能假装已经关掉
- 这套“窗口差异快照 + 只关闭本轮新开的目标页窗口 + 快照缺失时禁止盲关”的收尾规则，视为千帆后台公用主干能力；后续新增千帆任务默认优先复用，不要散落到各个业务脚本里重复实现
- 这条正式任务即使从 `current` 入口执行，运行状态、计划文件和导出缓存也必须固定回写根目录共享 `runtime/`，不要落到发布快照自己的 `current/runtime`

## 新网站站点分型

- 以后这个仓库如果新增网站、工作台或内部工具网页，默认先按“站点分型”决定长期运行方案，不要每次临时发明一套
- 默认优先按 3 类长期方案评估：`云端正式站`、`本机正式站`、`本机权限例外站`
- 默认决策顺序固定是：先判断能不能做成云端正式站；只有确实依赖本机真值时，才允许做成本机正式站；只有明确命中 macOS 权限限制时，才允许归为本机权限例外站

### 云端正式站

- 默认优先方案
- 适合不依赖本机文件、本机素材库、本机桌面软件、本机权限环境的网站
- 正式运行应放到云服务器
- 重启恢复、常驻、守护统一交给云端方案处理
- 这类网站不要再依赖用户电脑常开

### 本机正式站

- 只用于必须依赖这台 Mac 本机文件、Eagle、桌面软件或本机运行环境的网站
- 正式运行统一走“正式版本目录 + 本机自动恢复守护”
- 不允许继续靠手动跑脚本维持可用
- 不允许长期保留第二个正式副本目录或另一套并行正式站

### 本机权限例外站

- 只用于明确受 macOS 权限限制、必须从用户会话拉起才能稳定工作的站
- 这类站允许保留“用户会话拉起 + 轻量 watchdog”
- 但必须在仓库维护文档里显式标记为“权限例外”
- 不能悄悄演变成另一套未说明的长期方案

## 新增第 4 类方案的前置条件

- 这 3 类是当前默认优先方案，但不是永久封死
- 如果后面业务上确实需要第 4 类新方案，必须先明确说明为什么现有 3 类不合适
- 还必须明确说明为什么必须新增方案，以及对稳定性、维护成本、本机负担的影响
- 只有得到用户明确确认后，才允许开发、落文档、上线和后续维护
- 在用户明确确认之前，不允许偷偷把新网站做成第 4 套、第 5 套长期运行方案
- 也不允许把临时排障做法直接写成新的正式主线

## 新网站上线要求

- 新网站上线时，必须把“它属于哪一类站点”写进维护文档
- 新网站如果是本机正式站或本机权限例外站，必须同时补自动恢复方案，不能留到以后再补
- 能上云的尽量上云，避免用户电脑长期挂越来越多正式服务

## 正式入口约束

允许：

- 已承诺稳定的 CLI 脚本
- 固定服务别名
- 固定 HTTP 地址
- 正式环境 `current` 入口

禁止：

- 仓库镜像
- 临时副本
- worktree
- 手工复制目录
- 历史目录
- 聊天里提到的随手路径

## 维护人需要记住的事

- 正式发布前自动先跑治理校验
- GitHub 是代码备份，不是真实业务数据备份
- GitHub 自动备份继续固定 `10:00` 主跑、`10:20` 巡检，安装一次后不需要每天重新安装或手动触发
- GitHub 备份除了 `runtime/`、`releases/`、`current`、`release-log/`、`.github_backup_logs/`，还要继续拦截 `.tmp/`、`.next/`、调试截图、根目录临时导出物，以及名字里带 `secret` / `token` / `credential` 的内容
- 回滚只切代码，不碰 `runtime/`
- 当前长期只保留 `main`
